services:
  website:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./nginx-dev.conf:/etc/nginx/conf.d/default.conf:ro
      - ./website:/usr/share/nginx/html/website:ro
      - ./biolink:/usr/share/nginx/html/biolink:ro
    restart: unless-stopped
